# Overgrown 🌼

A cozy pixel-art tactics roguelike. Lead a band of meadow critters against the
Bramble — every friend moves in a pattern you might just recognize, but nobody
here has ever heard of chess.

Sister project to Cozy Sprites. See `DESIGN.md` for the vision and `PLAN.md`
for build status.

```bash
npm install
npm run dev      # dev server
npm test         # unit tests (Vitest)
npm run build    # typecheck + production build
```

Deploy: static site — import the repo on Vercel with the Vite preset, done.
